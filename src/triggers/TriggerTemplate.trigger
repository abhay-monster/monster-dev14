/*
** @author : Cloud Sherpas Inc.
** @date : MM/DD/YYYY
** @description : <Object> Trigger for all CRUD events. 
*/
    //note - Based on Trigger Template created by Mike Leach http://www.embracingthecloud.com/2010/07/08/ASimpleTriggerTemplateForSalesforce.aspx

trigger TriggerTemplate on Account (after delete, after insert, after undelete, after update, before delete, before insert, before update) {
/*
    // declare handler  
    //AccountTriggerHandler handler = new AccountTriggerHandler();
          
    if(Trigger.isInsert && Trigger.isBefore){
        // before insert event      
    }
    else if(Trigger.isInsert && Trigger.isAfter){
        // after insert event
    }
    else if(Trigger.isUpdate && Trigger.isBefore){
        // SAMPLE IMPLEMENTATION
        // before update event
        //try{
            // create new instance of AccountTriggerHandler
            // handler = new AccountTriggerHandler(Trigger.isExecuting, Trigger.size);
            
            // call the on before update event handler
            // handler.OnBeforeUpdate(Trigger.old, Trigger.new, Trigger.newMap, Trigger.oldMap);
        //}
        //catch(BaseApplicationException bae){
        //   for(Integer iLoop=0;iLoop<Trigger.size;iLoop++)
        //    Trigger.old[iLoop].addError(bae.UserFriendlyMessage);
        //}
    }
    else if(Trigger.isUpdate && Trigger.isAfter){
        // after update event       
    }
    else if(Trigger.isDelete && Trigger.isBefore){
        // before delete event        
    }
    else if(Trigger.isDelete && Trigger.isAfter){ 
        // after delete event        
    }
    else if(Trigger.isUnDelete){ 
        // undelete event        
    }   */
}